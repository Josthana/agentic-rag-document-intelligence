apiVersion: v1
kind: Service

metadata:
  name: agentic-rag-api-service

spec:
  type: NodePort

  selector:
    app: agentic-rag-api

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080